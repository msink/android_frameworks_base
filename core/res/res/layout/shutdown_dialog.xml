<?xml version="1.0" encoding="utf-8"?>

<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="200dip"
    android:layout_height="wrap_content"
    android:gravity="center"
    android:orientation="vertical" >

    <LinearLayout
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:gravity="center_horizontal"
        android:orientation="vertical" >
        <ImageView android:id="@+id/shutdown_dialog_icon"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dip"
            android:background="@drawable/shutdown_icon" />
        <TextView android:id="@+id/shutdown_dialog_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="20dip"
            android:textSize="27sp"
            android:textStyle="bold"
            android:textColor="#000"
            android:text="@string/power_off" />
    </LinearLayout>

    <ListView android:id="@+id/shutdown_dialog_view"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:paddingLeft="5dip"
        android:paddingTop="20dip"
        android:paddingRight="5dip"
        android:gravity="center_horizontal"
        android:orientation="vertical"
        android:listSelector="@drawable/list_selector_background_select"
        android:divider="@drawable/listview_solid_line"
        android:dividerHeight="2dip" />

</LinearLayout>